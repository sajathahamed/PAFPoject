$env:JAVA_HOME = "$env:USERPROFILE\scoop\apps\openjdk17\current"; $env:PATH = "$env:JAVA_HOME\bin;$env:USERPROFILE\maven\apache-maven-3.9.6\bin;$env:PATH"; mvn spring-boot:run

# Google OAuth (server redirect — used by "Continue with Google" on the login page)
# Set before mvn spring-boot:run (same OAuth client as in Google Cloud):
#   $env:GOOGLE_CLIENT_ID = "....apps.googleusercontent.com"
#   $env:GOOGLE_CLIENT_SECRET = "...."
#
# In Google Cloud Console > APIs & Services > Credentials > your OAuth 2.0 Client ID:
#   Authorized redirect URIs MUST include:
#     http://localhost:8080/login/oauth2/code/google
#   (Vite proxies /oauth2 to the backend; Google returns to port 8080.)
#
# Frontend runs on http://localhost:5174 (see vite.config.js). CORS already allows 5174 in application.yml.